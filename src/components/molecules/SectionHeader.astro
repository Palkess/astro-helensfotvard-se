---
interface Props {
    title: string;
    subtitle?: string;
}
const { title, subtitle } = Astro.props;
---

<h2 class="text-4xl font-semibold text-stone-900 text-center mb-4">{title}</h2>
{subtitle && <p class="text-lg text-stone-500 text-center mb-14 max-w-xl mx-auto">{subtitle}</p>}
